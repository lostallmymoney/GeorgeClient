# George Client

George Client is a Fabric utility mod.

## Building

Run:

```bash
./gradlew build
```

## Notes

- The internal Java package namespace is still `meteordevelopment.meteorclient` for compatibility.
- The runtime mod identity, resource namespace, and build artifact name are `george-client`.
